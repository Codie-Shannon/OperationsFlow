<section class="of-metric-grid @Class">
    @ChildContent
</section>

@code {
    [Parameter] public string Class { get; set; } = string.Empty;
    [Parameter] public RenderFragment? ChildContent { get; set; }
}